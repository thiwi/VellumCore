{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "vellum-core.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: {{ include "vellum-core.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
